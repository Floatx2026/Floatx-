# Sanity CMS — values from sanity.io/manage > Project settings
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-12-01

# Server-only write token (used by scripts/import-opportunities.ts).
# Generate at sanity.io/manage > API > Tokens (Editor or Write permission).
# NEVER commit this. NEVER prefix with NEXT_PUBLIC_.
SANITY_WRITE_TOKEN=
